version: '3.8'

services:
  refair-env:
    build: .
    container_name: refair_python
    volumes:
      # Espelha a sua pasta atual do Windows para dentro do Docker
      - .:/app
    ports:
      - "8888:8888"
      - "5000:5000"
    stdin_open: true
    tty: true